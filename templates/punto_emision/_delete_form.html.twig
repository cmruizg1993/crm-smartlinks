<form method="post" action="{{ path('punto_emision_delete', {'id': punto_emision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ punto_emision.id) }}">
    <button class="btn">Delete</button>
</form>
