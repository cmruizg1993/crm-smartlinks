{% extends 'base.html.twig' %}

{% block title %}PuntoEmision{% endblock %}

{% block body %}
    <h1>PuntoEmision</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ punto_emision.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ punto_emision.codigo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ punto_emision.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('punto_emision_index') }}">back to list</a>

    <a href="{{ path('punto_emision_edit', {'id': punto_emision.id}) }}">edit</a>

    {{ include('punto_emision/_delete_form.html.twig') }}
{% endblock %}
