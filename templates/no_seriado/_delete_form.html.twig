<form method="post" action="{{ path('no_seriado_delete', {'id': no_seriado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ no_seriado.id) }}">
    <button class="btn">Delete</button>
</form>
