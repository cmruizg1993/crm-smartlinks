{% extends 'base.html.twig' %}

{% block title %}New Cliente{% endblock %}

{% block body %}
    <div class="row bg-gradient-info  p-2 m-2 text-white justify-content-end">
        <div class="col">
            <h3>Nuevo Cliente</h3>
        </div>
        <div class="col">
            <a class="btn btn-xs btn-secondary" style="float: right" href="{{ path('cliente_index') }}" title="Atrás">
                <i class="mdi mdi-keyboard-backspace"></i>
            </a>
        </div>
    </div>
    {{ include('cliente/_form.html.twig',{'submit': 'SI'}) }}


{% endblock %}
