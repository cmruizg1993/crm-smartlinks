<form method="post" action="{{ path('app_secuencial_delete', {'id': secuencial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secuencial.id) }}">
    <button class="btn">Delete</button>
</form>
