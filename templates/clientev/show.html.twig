{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table table-bordered table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente.id }}</td>
            </tr>
            <tr>
                <th>Nombres</th>
                <td>{{ cliente.nombres }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cliente.email }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ cliente.telefono }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cliente.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clientev_index') }}">back to list</a>

    <a href="{{ path('clientev_edit', {'id': cliente.id}) }}">edit</a>

    {{ include('clientev/_delete_form.html.twig') }}
{% endblock %}
