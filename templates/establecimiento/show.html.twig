{% extends 'base.html.twig' %}

{% block title %}Establecimiento{% endblock %}

{% block body %}
    <h1>Establecimiento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ establecimiento.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ establecimiento.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ establecimiento.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ establecimiento.direccion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('establecimiento_index') }}">back to list</a>

    <a href="{{ path('establecimiento_edit', {'id': establecimiento.id}) }}">edit</a>

    {{ include('establecimiento/_delete_form.html.twig') }}
{% endblock %}
