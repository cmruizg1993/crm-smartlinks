{% extends 'base.html.twig' %}

{% block title %}EstadoOrden index{% endblock %}

{% block body %}
    <h1>EstadoOrden index</h1>

    <table class="table table-bordered table-responsive">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Codigo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for estado_orden in estado_ordens %}
            <tr>
                <td>{{ estado_orden.id }}</td>
                <td>{{ estado_orden.nombre }}</td>
                <td>{{ estado_orden.codigo }}</td>
                <td>
                    <a href="{{ path('estado_orden_show', {'id': estado_orden.id}) }}">show</a>
                    <a href="{{ path('estado_orden_edit', {'id': estado_orden.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('estado_orden_new') }}">Create new</a>
{% endblock %}
