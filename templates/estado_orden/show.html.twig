{% extends 'base.html.twig' %}

{% block title %}EstadoOrden{% endblock %}

{% block body %}
    <h1>EstadoOrden</h1>

    <table class="table table-bordered table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_orden.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estado_orden.nombre }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ estado_orden.codigo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estado_orden_index') }}">back to list</a>

    <a href="{{ path('estado_orden_edit', {'id': estado_orden.id}) }}">edit</a>

    {{ include('estado_orden/_delete_form.html.twig') }}
{% endblock %}
