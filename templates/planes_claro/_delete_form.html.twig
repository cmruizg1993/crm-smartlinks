<form method="post" action="{{ path('planes_claro_delete', {'id': planes_claro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planes_claro.id) }}">
    <button class="btn">Delete</button>
</form>
