{% extends 'base.html.twig' %}

{% block title %}PlanesClaro{% endblock %}

{% block body %}
    <h1>PlanesClaro</h1>

    <table class="table table-bordered table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planes_claro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ planes_claro.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planes_claro_index') }}">back to list</a>

    <a href="{{ path('planes_claro_edit', {'id': planes_claro.id}) }}">edit</a>

    {{ include('planes_claro/_delete_form.html.twig') }}
{% endblock %}
