{% extends 'base.html.twig' %}

{% block title %}PlanesClaro index{% endblock %}

{% block body %}
    <h1>PlanesClaro index</h1>

    <table class="table table-bordered table-responsive">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for planes_claro in planes_claros %}
            <tr>
                <td>{{ planes_claro.id }}</td>
                <td>{{ planes_claro.nombre }}</td>
                <td>
                    <a href="{{ path('planes_claro_show', {'id': planes_claro.id}) }}">show</a>
                    <a href="{{ path('planes_claro_edit', {'id': planes_claro.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('planes_claro_new') }}">Create new</a>
{% endblock %}
