{% extends 'base.html.twig' %}

{% block title %}Impuesto{% endblock %}

{% block body %}
    <h1>Impuesto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ impuesto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ impuesto.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ impuesto.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('impuesto_index') }}">back to list</a>

    <a href="{{ path('impuesto_edit', {'id': impuesto.id}) }}">edit</a>

    {{ include('impuesto/_delete_form.html.twig') }}
{% endblock %}
