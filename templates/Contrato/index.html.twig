{% extends 'base.html.twig' %}

{% block title %}Contratos{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h3 class="m-2">Contratos</h3>
        </div>
        <div class="col d-flex justify-content-end">
            <a class="btn btn-sm btn-success" href="{{ path('contrato_activacion') }}">
                Generar Activación
            </a>
            <a class="btn btn-sm btn-warning" href="{{ path('contrato_corte') }}">
                Generar Corte
            </a>

            <a href="{{ path('contrato_new') }}" class="btn btn-primary btn-sm">
                <strong><b>+</b></strong>Nuevo
            </a>
        </div>
    </div>


    <contratos listado="{{ contratos|json_encode }}"></contratos>

{% endblock %}
{% block javascript %}

{{ encore_entry_script_tags('app') }}
{% endblock %}
