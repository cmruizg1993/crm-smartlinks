{% extends 'base.html.twig' %}

{% block title %}Órdenes{% endblock %}

{% block body %}

    <div class="container">

        <div class="row mb-3">
            <div class="col-2">
                <h3>Órdenes</h3>
            </div>
        </div>
    </div>
    <ordenes listado="{{ ordenes|json_encode }}"
              urledicion="{{ path('orden_edit',{id: 0}) }}"
              urlmostrar="{{ path('orden_show', {id: 0}) }}"
    ></ordenes>
{% endblock %}
{% block javascript %}

{{ encore_entry_script_tags('app') }}
{% endblock %}

