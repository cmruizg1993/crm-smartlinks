<form method="post" action="{{ path('app_cuenta_por_cobrar_delete', {'id': cuenta_por_cobrar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cuenta_por_cobrar.id) }}">
    <button class="btn">Delete</button>
</form>
