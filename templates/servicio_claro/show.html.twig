{% extends 'base.html.twig' %}

{% block title %}ServicioClaro{% endblock %}

{% block body %}
    <h1>ServicioClaro</h1>

    <table class="table table-bordered table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio_claro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ servicio_claro.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('servicio_claro_index') }}">back to list</a>

    <a href="{{ path('servicio_claro_edit', {'id': servicio_claro.id}) }}">edit</a>

    {{ include('servicio_claro/_delete_form.html.twig') }}
{% endblock %}
